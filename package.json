{
    "source": "src/index.html",
    "scripts": {
        "start": "parcel",
        "build": "parcel build",
        "buildr": "npm run build && npm run start",
        "watch": "nodemon --exec npm run buildr",
        "commit": "cz"
    },
    "browserslist": "> 0.5%, last 2 versions, not dead",
    "dependencies": {
        "dotenv": "^16.0.1"
    },
    "devDependencies": {
        "buffer": "^6.0.3",
        "commitizen": "^4.2.5",
        "cz-emoji": "^1.3.2-canary.2",
        "nodemon": "^2.0.19",
        "os-browserify": "^0.3.0",
        "parcel": "^2.6.2",
        "path-browserify": "^1.0.1",
        "process": "^0.11.10"
    },
    "config": {
        "commitizen": {
            "path": "./node_modules/cz-emoji"
        }
    }
}
